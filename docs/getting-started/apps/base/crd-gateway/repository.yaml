apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: OCIRepository
metadata:
  name: crd-gateway
  namespace: openmfp-system
spec:
  interval: 5m
  url: oci://ghcr.io/openmfp/crd-gateway/helm/crd-gateway
  ref:
    semver: "*"
  secretRef: # TODO: temporary as long as the repo is private
    name: ghcr-credentials