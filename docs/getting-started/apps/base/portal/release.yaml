apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: portal
  namespace: openmfp-system
spec:
  interval: 5m
  chartRef:
    kind: OCIRepository
    name: portal
  releaseName: portal
  values:
    image:
      pullSecret: ghcr-credentials # TODO: temporary as long as the repo is private
    istio:
      enabled: false
    externalSecrets:
      enabled: false
    trust:
      openmfp:
        baseDomains: "localhost"
        authDomain: http://localhost:8080/keycloak/realms/openmfp/protocol/openid-connect
        oidcClientSecretName: openmfp-client
        loginAudience: openmfp