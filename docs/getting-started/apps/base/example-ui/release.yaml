apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: example-ui
  namespace: openmfp-system
spec:
  interval: 5m
  chartRef:
    kind: OCIRepository
    name: example-ui
  releaseName: example-ui
  dependsOn:
    - name: istio-istiod
      namespace: istio-system